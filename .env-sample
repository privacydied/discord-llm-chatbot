# ===== DISCORD BOT CONFIGURATION =====
# Required: Discord bot token from https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here

# Optional: Discord bot command prefix (default: !)
COMMAND_PREFIX=!

# Optional: Discord bot owner IDs (comma-separated)
OWNER_IDS=123456789012345678,987654321098765432

# Optional: Enable message content intent (default: true)
ENABLE_MSG_INTENT=true

# ===== AI BACKEND CONFIGURATION =====
# Choose the backend for TEXT chat ('openai' for OpenAI/OpenRouter, 'ollama' for local Ollama)
TEXT_BACKEND=openai

# ===== OPENAI / OPENROUTER SETTINGS =====
# Required when TEXT_BACKEND=openai: OpenAI or OpenRouter API key
OPENAI_API_KEY=your_openai_or_openrouter_api_key

# Optional: OpenAI API base URL (default: https://api.openai.com/v1)
# For OpenRouter: https://openrouter.ai/api/v1
OPENAI_API_BASE=https://openrouter.ai/api/v1

# Optional: OpenAI text model (default: gpt-4)
OPENAI_TEXT_MODEL=deepseek/deepseek-chat-v3-0324:free

# Legacy model setting (deprecated, use OPENAI_TEXT_MODEL instead)
#OPENAI_MODEL=gpt-4

# ===== OLLAMA SETTINGS =====
# Optional: Ollama base URL (default: http://localhost:11434)
#OLLAMA_BASE_URL=http://localhost:11434

# Optional: Ollama model name (default: llama3)
#OLLAMA_MODEL=llama3

# ===== AI BEHAVIOR CONFIGURATION =====
# Optional: Temperature for AI responses (0.0-1.0, default: 0.7)
TEMPERATURE=0.7

# Optional: Top-p sampling parameter (0.0-1.0, default: 0.9)
TOP_P=0.9

# Optional: Frequency penalty (-2.0 to 2.0, default: 0.0)
FREQUENCY_PENALTY=0.0

# Optional: Presence penalty (-2.0 to 2.0, default: 0.0)
PRESENCE_PENALTY=0.0

# Optional: Request timeout in seconds (default: 30)
DEFAULT_TIMEOUT=30

# Optional: Maximum context length in tokens (default: 4000)
MAX_CONTEXT_LENGTH=4000

# Optional: Maximum response tokens (default: 1000)
MAX_RESPONSE_TOKENS=1000

# ===== MEMORY & CONVERSATION MANAGEMENT =====
# Optional: Maximum memories per user (default: 100)
MAX_MEMORIES=100

# Optional: Maximum memories per server (default: 100)
MAX_SERVER_MEMORY=100

# Optional: Auto-save interval in seconds (default: 300 = 5 minutes)
MEMORY_SAVE_INTERVAL=300

# ===== TEXT-TO-SPEECH CONFIGURATION =====
# Optional: Whisper model for speech recognition (default: base)
WHISPER_MODEL=base

# ===== FILE & DIRECTORY CONFIGURATION =====
# Optional: Temporary files directory (default: temp)
TEMP_DIR=temp

# Optional: User profiles directory (default: user_profiles)
USER_PROFILE_DIR=user_profiles

# Optional: Server profiles directory (default: server_profiles)
SERVER_PROFILE_DIR=server_profiles

# Optional: User logs directory (default: user_logs)
USER_LOGS_DIR=user_logs

# ===== LOGGING CONFIGURATION =====
# Optional: Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL, default: INFO)
LOG_LEVEL=INFO

# ===== DEVELOPMENT & DEBUGGING =====
# Optional: Enable debug mode (default: false)
DEBUG=false

# Optional: Enable verbose logging (default: false)
VERBOSE=false


