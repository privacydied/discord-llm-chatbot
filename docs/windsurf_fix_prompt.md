# Windsurf Fix Instructional Prompt

You are working inside the discord-llm-chatbot repository. Your goal is to eliminate the beep-only output from text-to-speech synthesis while preserving every existing pathway and ensuring routing and gating remain consistent across all input modalities (Discord message triggers, command invocations, and any Flask or service-based TTS entry points). Follow the directives below meticulously and do not introduce regressions.

1. Fully audit `bot/tts/interface.py`, `bot/tts/__init__.py`, `bot/tts/generation.py`, `bot/tts/engines/`, `bot/speak/`, and any other module that influences TTS initialization, engine selection, or synthesis fallbacks. Confirm how environment configuration, lazy loading, and error handling determine which engine is active.
2. Identify every branch that defaults to `StubEngine` or any placeholder audio. Trace how those branches are triggered during cold start, during asset validation, and during runtime failures. Document and rationalize each pathway; remove only the unnecessary or incorrect fallbacks while preserving safe recovery behavior.
3. Ensure that `TTS_ENGINE` remains the sole environment variable controlling engine selection. Honor its documented default (`kokoro-onnx`) when not explicitly set, while still providing a safe fallback pathway that surfaces actionable diagnostics rather than silently emitting placeholder audio. Do not create any new environment variables.
4. Guarantee that Kokoro asset acquisition (`ensure_kokoro_assets` and related helpers) runs exactly once per engine activation, handles missing files gracefully, and produces clear logs when downloads fail. Make sure any lazy upgrade from stub to Kokoro happens deterministically and never leaves the manager stuck on the stub when assets are available.
5. During synthesis, propagate meaningful errors or status objects instead of silently substituting the stub output. If a fallback must occur, log it at an appropriate level and emit metadata so callers can display warnings. Ensure the routing logic is mirrored consistently for both synchronous and asynchronous call sites.
6. Align all higher-level helpers (`bot.tts.generate_tts`, `bot.speak.speak_infer`, Discord command handlers, REST endpoints) with the corrected manager behavior. Every modality should honor the same gating: respect configured engine, surface explicit failures, and only fall back when recovery is safe and expected.
7. Update tests or add new coverage that verifies: (a) default startup selects Kokoro when assets exist; (b) missing assets trigger a single, well-logged recovery flow; (c) runtime synthesis errors do not silently emit placeholder audio; (d) all public entry points react consistently. Keep test setup simple and reuse existing fixtures.
8. Review all logs and documentation for accuracy. Update any misleading guidance so operators know how to configure engines, fetch assets, and diagnose issues. Maintain existing documentation structure and avoid introducing redundant sections.
9. Do not introduce unrelated refactors. Keep functionality simple, target the bug directly, and retain existing public APIs. Avoid over-engineering, new abstractions, or speculative features.
10. Perform thorough manual verification by exercising each entry point with valid and invalid configurations, confirming that the beep-only output is eliminated and that fallback behavior matches the documented expectations.

Deliverables should be a cohesive, well-tested fix that restores real speech synthesis when Kokoro assets and configuration are present, while keeping routing and gating predictable for every modality.
