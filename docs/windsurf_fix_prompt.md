# Windsurf Fix Instructional Prompt

You are working inside the `discord-llm-chatbot` repository. Your mission is to restore the bot's implicit-context behaviour so that visual or media-only posts once again route through the "thoughts?" flow, mentions propagate prior context regardless of modality, and enhanced memory works uniformly across guild channels, threads, and DMs. Follow every directive precisely, keep functionality simple, and do **not** introduce new environment variables or unrelated refactors. Provide no code samples in your response.

1. Begin by auditing the multimodal entry point in `bot/router.py`, especially `_process_multimodal_message_internal`, `_has_meaningful_text`, and the precedence gates for `VL_DEFAULT_PROMPT_FOR_BARE_IMAGE` and `ROUTING_WEB_LINK_PRECEDENCE`. Confirm how `collect_input_items`, `reply image harvest`, and the adoption fallbacks populate `original_text`. Identify where empty captions or mention-only triggers currently fall through to the "no text" persona instead of defaulting to the "ack+thoughts" treatment.
2. Reinstate the implicit "thoughts?" nudge when a message contains only vision, audio/video, or document inputs. If `original_text` is blank but the aggregator has at least one successful `InputItem`, guarantee that the downstream call into `_flow_process_text` receives a concise user prompt (e.g., the existing `reply_style="ack+thoughts"` path) instead of raw evidence with no guidance. Reuse existing helpers—prefer adjusting the aggregator/prompt composition rather than inventing a new control path.
3. Verify that `_flow_process_attachments_multimodal` and the per-item handlers (`_handle_image`, `_handle_video_url`, `_handle_audio_video_file`, `_handle_general_url`, etc.) still call `_flow_process_text` with both `context_str` and any `perception_notes`. Ensure the implicit question is appended **after** aggregating evidence so the persona never replies with "no text attached" when images or clips arrive without captions.
4. Revisit `bot/result_aggregator.py`. Confirm that `get_aggregated_prompt` preserves `original_text` when present and now also injects the implicit "thoughts?" style request when `original_text` is empty but at least one successful item exists. Maintain the existing provenance headers and success/failure accounting; do not strip or reorder entries.
5. Inspect `bot/modality.py` to ensure `collect_input_items` still returns attachment, URL, and embed items even when message content is empty. Confirm that reply-harvested assets (from `_process_multimodal_message_internal`) survive deduplication and respect ordering so the restored implicit prompt applies equally to reply threads, DM replies, and standalone posts.
6. Re-enable mention-context hydration across modalities. In `bot/router.py` review `_resolve_scope_and_target`, the invocation of `maybe_build_mention_context`, `collect_thread_tail_context`, and `collect_implicit_anchor_context`. Guarantee that regardless of whether the trigger payload is text, image, video, or voice, the resolved `context_str` (joined text from mention context or thread tail) is forwarded into `_invoke_text_flow` / `_flow_process_text` alongside the aggregated evidence.
7. Audit `bot/memory/mention_context.py` and `bot/memory/thread_tail.py` to ensure the collectors include attachment metadata flags (`has_attachments`, `attachment_summaries_if_available`) and jump URLs. Verify that the mention context packaging continues to run even when the triggering message has no textual content beyond the mention.
8. Validate that `bot/context.py` and `bot/memory/enhanced_context_manager.py` continue to persist prior turns for both guild and DM scopes. Confirm that message entries produced by the restored flow include the synthesized implicit prompt plus the AI's response so that subsequent mentions outside the original thread still recover the right history window.
9. Inspect the gating and persona prompts in `bot/brain.py`, `bot/contextual_brain.py`, and any system prompt loaders under `bot/config/` to confirm the "ack+thoughts" style remains wired for image/video-only conversations. Ensure that enhanced context injection does not strip the implicit question you add when `original_text` is empty.
10. Exercise routing across modalities: (a) bare image upload in a guild channel, (b) bare video upload in a DM, (c) replying to an existing screenshot with only a mention, (d) replying outside the original thread while tagging the bot. In each case confirm that the bot interprets the message as "thoughts?", consumes mention context, and responds without demanding additional text.
11. Update or extend targeted tests. Prioritize `tests/test_router_reply_routing.py`, `tests/memory/test_mention_context.py`, and any multimodal regression suites to cover media-only mentions and aggregated prompt injection. Keep fixtures lean; prefer augmenting existing parametrized cases.
12. Review logs and docs touched by the change. Ensure logging still distinguishes between bare attachments and explicit text prompts, and document the restored behaviour succinctly inside `docs/DEVNOTES_MENTION_UNIFICATION.md` or adjacent notes if necessary. Do not introduce new configuration toggles; rely on current flags (`VL_DEFAULT_PROMPT_FOR_BARE_IMAGE`, `ROUTING_WEB_LINK_PRECEDENCE`, etc.).
13. Run the relevant automated checks (`pytest` targets that cover router and memory flows, plus linting if configured) and perform manual Discord smoke tests to confirm there are no regressions in gating, retries, or modality detection.

Deliverables must be a cohesive fix that restores the implicit "thoughts?" behaviour, respects mention/context memory across all modalities, and maintains consistent routing and gating without over-engineering.
