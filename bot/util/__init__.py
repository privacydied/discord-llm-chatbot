"""Backwards-compat utilities namespace.

This package exists to preserve compatibility for legacy imports such as
`bot.util.logging`. It re-exports from the consolidated `bot.utils` package.
"""

# Intentionally minimal; modules under this package proxy to `bot.utils`.

