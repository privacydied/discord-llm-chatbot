# Core Dependencies
python-dotenv==1.0.1
discord.py==2.3.2
pip==25.1.1

# AI/ML
ollama==0.2.0
openai==1.94.0
kokoro-onnx>=0.4.9      # Kokoro ONNX TTS library
onnxruntime>=1.15.0
onnxruntime-gpu>=1.15.0  # Optional GPU support
spacy>=3.0.0            # Required for kokoro-onnx
num2words>=0.5.10       # Required for kokoro-onnx
soundfile>=0.10.0       # Required for kokoro-onnx
misaki[en]>=0.1.0       # Required for kokoro-onnx G2P

# Web & Networking
httpx==0.27.0
requests==2.31.0
beautifulsoup4==4.12.2
urllib3==2.2.1
playwright==1.42.0
trafilatura==1.6.2  # Web content extraction

# Caching & Utilities
cachetools==5.3.2
fake-useragent==1.4.0
pydantic>=1.10.0        # For configuration validation
typing-extensions>=4.0.0  # Python 3.11 compatibility
PyNaCl==1.5.0

# Document Processing
PyPDF2>=3.0.0
pdfminer.six>=20221105
python-docx>=1.0.1
python-magic>=0.4.27
PyMuPDF>=1.23.8
pytesseract>=0.3.10
Pillow>=10.2.0

# Audio Processing (for TTS)
soundfile>=0.12.1
numpy>=2.0.2

# System Dependencies (may need manual installation)
# - libsndfile1 (required by soundfile)
#   Ubuntu/Debian: sudo apt-get install libsndfile1
#   RHEL/CentOS: sudo yum install libsndfile
#   Alpine: apk add alsa-lib

# Speech-to-text dependencies
ctranslate2>=4.0.0
faster-whisper>=1.0.0
torch>=2.0.0            # PyTorch for faster-whisper
torchaudio>=2.0.0      # Required for audio processing
ffmpeg-python>=0.2.0
aiofiles>=23.0.0        # async file I/O
pydub>=0.25.1           # audio conversion
aiohttp==3.9.3          # Async HTTP client for TTS microservice
